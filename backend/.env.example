# Database
DB_HOST="localhost"
DB_PORT=5432
DB_NAME="securejob"
DB_USER=""
DB_PASSWORD=""

# Security — generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=""

# Set to true for development (enables Swagger docs, SQL echo)
DEBUG=false

# SMTP Email (optional — OTP prints to console if not set)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM=""
